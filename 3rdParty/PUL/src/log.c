/* Poly
   Useful
   Lib */

#include <pul/types.h>
#include <stdio.h>

#define LOG_BUFFER 65536

void puInitLog()
{
/*	FILE* fp;

	if( fp = fopen( "DebugLog.txt", "w" ) )
		fclose( fp );*/
}

puCloseLog()
{

}

void puWriteLogEntry( PUU8* _pCategory, PUU8* _pMessage, PUU32 _Param1, PUU32 _Param2, PUU32 _Param3, PUU32 _Param4 )
{
/*	PUU8 TmpStr[256], TmpStr2[256];

	sprintf( TmpStr, _pMessage, _Param1, _Param2, _Param3, _Param4 );
	sprintf( TmpStr2, "%s: %s\n", _pCategory, TmpStr );*/
}